#include <stdlib.h>
#include <stdint.h>

int save_ppm_file(uint32_t *pixels, size_t width, size_t height)
{
    if (!pixels || width == 0 || height == 0)
    {
        return -1;
    }

    
}

int main(void)
{
}